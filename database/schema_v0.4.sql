-- =============================================================
-- 数据中心机房准入系统 v0.4：访客登录（手机号 + 密码）
-- 对库 dc_access_system 执行
-- =============================================================

-- 访客 users 表加密码列（老访客密码为空，需重新注册或手动设置）
ALTER TABLE users ADD COLUMN password VARCHAR(128) DEFAULT '' NOT NULL;
